---
- name: check for installation of pach
  become: yes
  stat:
    path: "/usr/share/kubernetes/{{ pach_fullname }}"
  changed_when: false
  register: pach_dir

- when: not pach_dir.stat.exists
  block:
    - name: download...
      become: yes
      get_url:
        url: 'https://github.com/pachyderm/pachyderm/releases/download/v1.6.9/pachctl_1.6.9_linux_amd64.tar.gz'
        dest: "/tmp/{{ pach_tgz }}"
        checksum: "{{ pach_checksum }}"
        mode: 0644

    - name: make install dir
      become: yes
      file:
        path: '/usr/share/kubernetes'
        state: directory
        mode: 0755

    - name: unarchive
      become: yes
      unarchive:
        copy: no
        src: "/tmp/{{ pach_tgz }}"
        dest: '/usr/share/kubernetes'
        creates: "/usr/share/kubernetes/{{ pach_fullname }}"

  always:
    - name: cleanup...
      become: yes
      file:
        path: "/tmp/{{ pach_tgz }}"
        state: absent

- name: Install Pachyderm via Helm
  become: yes
  shell: helm install --namespace pachyderm --name pachyderm stable/pachyderm
  args:
    executable: /bin/bash

- name: Allow services to act as admin general bypass for Pachyderm
  become: yes
  shell: kubectl create clusterrolebinding serviceaccounts-cluster-admin --clusterrole=cluster-admin --group=system:serviceaccounts
  args:
    executable: /bin/bash
